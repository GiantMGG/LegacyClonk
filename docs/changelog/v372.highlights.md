**What to try** — five things to click in v372:

1. **Haul a hundred** — playing with the mouse, drag a frame over a loot pile:
   selection now takes up to 100 loose objects (was 20), and a clonk told to
   carry them queues up to 200 orders instead of freezing mid-list. If you
   somehow hit the new ceiling, the log names the object instead of silently
   dropping commands.
   (ASSERT: SelectionLimits + smoke_SelectionLimitsSmoke — both CTest-green,
   mutation-red at the old caps: smoke fails at call #36 under 35; pin fails
   20 == 100; battery 128/128, cycle-165 closure note.)
2. **Rebind without surprises** — Options → Bindings: binding the same key to
   two actions now asks first. The conflict dialog names the physical key and
   warns when both actions would fire on one press. And every player file
   remembers its own preset, so hot-seat players stop overwriting each other.
   (ASSERT: C4ControlPresets — ConflictScan pins (mutation-red: detection off →
   RED) + per-player Preset= persistence pins, cycle-154/155 closure notes;
   LanguageTables byte pin, cycle-163.)
3. **Pin down the map** — start any random-map scenario (Free Game works too):
   the world-settings stage of the pre-game dialog now has a seed field. Type
   a number, step it with the arrows, watch the live preview re-roll to
   match. A bad map no longer means gambling on restarts.
   (ASSERT: landscape_seed_smoke + CharInputRouting pin — typed entry proven
   end-to-end ("55" → field 55 → seed 55), cycles 158–162 closure notes.)
4. **Three small mercies** — a failed direct join now shows why it failed
   before closing; Dig while climbing lets go of the wall without
   phantom-dropping your held item; and Alt+Tab in fullscreen should now
   yield focus on Windows — a long-standing complaint; this is the first
   build with the fix, so please confirm it behaves (cycle-161).
   (ASSERT: join_failure_message_pin + climb_dig_release_smoke — both
   CTest-green, mutation-red; alt-tab: Win32 fix `da2ec789` landed, runtime
   check rides this bullet, cycle-161 note.)
5. **Hear the homestead** — FirstLight's farmstead now talks back: the mill
   grinds, the sickle whispers through the wheat, the fishtrap splashes, the
   market bell chimes. Five CC0 sounds wired where the mission earns them.
   (ASSERT: FirstLightSoundSmoke + SoundExists probe pins — MUT-red ×2,
   cycle-164 closure note.)

---

- Modern controls, part four: rebinding warns about key conflicts, every
  player file keeps its own preset, and Linux builds now compile the gamepad
  layer (the released linux64 binary re-grep lands with this release's
  verification).
- Mouse mass-play: drag-select up to 100 objects, command stacks up to 200
  entries deep.
- Round setup: the map seed is editable and live.
- Fixes: join-failure dialog, climb-release item drop, Windows fullscreen
  Alt+Tab.
- Sound pass round one: FirstLight mission sounds.

---

*LegacyClonk and this fork:* LegacyClonk is a fan-made continuation of the
Clonk Rage engine originally developed by RedWolf Design. This project is a
friendly fork of the LegacyClonk project; both continue the same engine
independently. Our release numbers count this fork's releases and are not
synchronized with any other project's numbering (fork v372 ≠ upstream vNNN).
Compatibility with existing Clonk Rage content remains a core commitment.
