**What to try** — three things to click in v370:

1. **Learn from the tutorial** — Start Game → Tutorial → Tutorial01: the tutorial now
   speaks; guided message boxes walk you through your first steps (v369 shipped it silent).
2. **Blueprints reach the round** — round setup → Store tab (Ctrl+Tab) → tick Windmill →
   Start: the windmill is buildable in the live round.
3. **Map scale** — world settings → the "Map scale" row (formerly "Zoom factor"): a clean
   "10 ×" readout with no garbled unit glyph.

---

- The new-player path is fixed end to end: the tutorials' helper scripts link again — a
  one-character launch-path defect had silently un-linked all 213 guidance calls across
  the ten tutorials — and a new link gate keeps the covered Tutorial.c4f and Worlds.c4f
  scenarios free of unresolved identifiers (two scenarios with known script debt are
  explicitly skipped and tracked).
- The pre-game settings dialog is rebuilt: the Start button is on screen again, the store
  gained a dense wrap grid with count editors for goods, restock and blueprints, and
  authored scenarios without configurable options skip the dialog entirely.
- A Free Game button on the main menu jumps straight into round setup on the
  world-settings stage.
- The quickstart link and the first-game guide match the real game: the bundled guide
  opens from the help button, its keys and menu names are correct, and Colony Bay's goal
  chain works.
- Round setup is translated: the new welcome and pre-game strings load from the string
  tables, German included.

---

*LegacyClonk and this fork:* LegacyClonk is a fan-made continuation of the
Clonk Rage engine originally developed by RedWolf Design. This project is
a friendly fork of the LegacyClonk project; both continue the same engine
independently. Our release numbers count this fork's releases and are not
synchronized with any other project's numbering (fork v369 ≠ upstream
vNNN). Compatibility with existing Clonk Rage content remains a core
commitment. — A full upstream-sync policy is being drafted.
