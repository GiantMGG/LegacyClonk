- The Breach: liquids now have bodies. Water piles up, drains at
  scenario-defined rates, and jets ballistically through breaches — every
  liquid carries its own FlowRate and JetFall values, and the Dammbruch
  showcase runs the whole arc with zero scripted water.
- Round setup reaches parity with the original generator's store surface:
  store goods, store restock, and construction blueprints sections, each
  with All/None toggles and count sliders — plus the matching --parameter
  keys for all three.
- The Start Game button is visible again, and the replay browser now lives
  in the main menu.
- Network reconnect is on by default, with a one-way version-gated
  migration for existing configurations.
- Scorpions return at 24x14 with a six-frame walk cycle — finally the
  right size next to a clonk.
- Custom key bindings survive a restart.

---

*LegacyClonk and this fork:* LegacyClonk is a fan-made continuation of the
Clonk Rage engine originally developed by RedWolf Design. This project is
a friendly fork of the LegacyClonk project; both continue the same engine
independently. Our release numbers count this fork's releases and are not
synchronized with any other project's numbering (fork v369 ≠ upstream
vNNN). Compatibility with existing Clonk Rage content remains a core
commitment. — A full upstream-sync policy is being drafted.
