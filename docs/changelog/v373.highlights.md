**What to try** — three things to click in v373:

1. **Watch the farm come to life** — Start Game → Missions → FirstLight: every object
   of the homestead loop now has real pixel art — fish trap, sickle, wheat seeds and
   sheaves, smoked fish, the smoking smokehouse, fishing net and rod, apple and apple
   seed (all of these shipped as blank boxes before). The pack's invisible-definition
   count drops from 18 to 8 — none of them Agriculture.
   (ASSERT: smoke_AgricultureArtSmoke — 10 defs spawn with post-rewire Picture rects +
   ActMap facets; gfx_check_FishTrap/Sickle/WheatSeed/WheatSheaf/SmokedFish/Smokehouse/
   FishingNet/FishingRod/Apple/AppleSeed — ten byte gates vs the generator, all Passed,
   cycle-168 closure note; visibility 18→8, scratch/168/visibility-final.txt.)
2. **Finish the mission** — FirstLight is winnable as shipped. The starting kit
   (2 fish traps, 6 wheat seeds, a sickle, 14 wood, 1 metal) grows a four-plant field
   and carries exactly the bill of materials for both production buildings — the
   windmill takes 7 wood + 1 metal, the smokehouse 7 wood — and the pond no longer
   freezes solid in winter; the freeze used to ice the fish trap dry and lock the
   fishery leg for good. All four ledger legs — wet trap, ripe field, turning mill,
   busy market — go green through two consecutive audits.
   (ASSERT: firstlight_scenario_gate — kit keys + resolvable components, 6 mutated-file
   variants rejected by the scratch harness; firstlight_climate_smoke — pond liquid at
   frames 11200/12600/14700, pre-fix iced by 12425; smoke_FirstLightSmoke — chapter and
   ledger replica; as-shipped balance run WIN frame=12600, seed 721. Basis: the
   end-to-end win is proven on a scripted mirror of the mission — same world builder,
   real timers, byte-identical kit; the real mission boots clean headless and in GUI,
   and the mirror places both buildings fully built, so the build act itself is pinned
   next cycle. Cycle-168 closure note.)
3. **Look closer at two classics** — the trade cog's hull reads as a ship (curved bow
   and stern, was slab-sided), and the salt-road scorpion's walk art shed its
   ear-dome artifacts.
   (ASSERT: ba_verdict_cog_v2 — paired before/after judgment PASS on the hull facet;
   vision_qa_scorpion — permanent battery GATE PASS on the changed art, cycle-167
   closure note.)

---

- FirstLight, the homestead flagship mission, is playable end to end: real art on all
  ten Agriculture objects it uses, a starting kit that can actually reach the goal,
  and a homestead pond that stays liquid through winter.
- Art polish: the trade cog gains a curved hull; the scorpion's walking band loses its
  ear-dome artifacts.
- The FirstLight mission contract — kit contents, resolvable construction components,
  climate — is now pinned in-tree, so edits that would silently break the mission fail
  CI instead.

---

*LegacyClonk and this fork:* LegacyClonk is a fan-made continuation of the
Clonk Rage engine originally developed by RedWolf Design. This project is a
friendly fork of the LegacyClonk project; both continue the same engine
independently. Our release numbers count this fork's releases and are not
synchronized with any other project's numbering (fork v373 ≠ upstream vNNN).
Compatibility with existing Clonk Rage content remains a core commitment.
