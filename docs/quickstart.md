# This page has moved

The modder quickstart tutorial has been replaced by
[First custom object](tutorials/first-object.md).

The new tutorial is structured as six time-boxed checkpoints with
verification gates, and ships a ready-made `GlowStone.c4d` skeleton.
