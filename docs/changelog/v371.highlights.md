**What to try** — five things to click in v371:

1. **Plug in a gamepad (Windows & macOS)** — gamepads now ship with Xbox-style
   default bindings: on a fresh install a recognized pad needs no setup — the
   left stick walks, A jumps, X digs. Entries saved as "unbound" by older
   versions self-heal to the new defaults on first load. Linux players sit
   this one out: this release's Linux build ships without pad support —
   bringing it there is next on the list. Pad-specific feel (deadzone, dpad
   interplay) is still being tuned — tell us how yours behaves.
   (ASSERT: C4GamepadDefaults pin incl. IniLoadMigratesExplicitMinusOneG2 —
   cycle-148 closure note; Linux caveat: released linux64 binary logs
   "Engine without Gamepad support" — scratch/149/verify/feuer.log.)
2. **Walk the Salt Road** — Start Game → The Salt Road → Act I — The Waiting
   Storm: a three-act desert campaign. Stock a dying oasis, hold it against the
   dusk scorpion raid, pick the ridge or the wadi through the storm, then settle
   the queen's toll by trade or by combat.
   (ASSERT: smoke_SaltRoadStorySmoke — pinned seed, three acts chain to
   SaltRoad_Ending; cycle-146 closure note.)
3. **Escape three rooms** — Start Game → Escape Rooms → The Siphon: physics
   escape puzzles — The Siphon, The Furnace and The Undertow — each with a
   headless-proven solution path.
   (ASSERT: EscapeSiphonSmoke / EscapeFurnaceSmoke / EscapeUndertowSmoke —
   cycle-147 closure note.)
4. **Watch the scorpions stride** — meet the brood on the Salt Road: a
   six-phase walking gait, both directions.
   (ASSERT: vision_qa_scorpion GATE PASS — ≥4 legs readable in all six phases;
   cycle-145 closure note.)
5. **See the windmill turn** — Start Game → Missions → Pearls: the windmill now
   shows its timber tower and turning wing. Out on the sea routes the trade cogs
   are no longer blank stubs — a visible mast and billowed sail shading.
   (ASSERT: WindmillArtSmoke + CogArtSmoke wiring pins; byte-gated
   gfx_check_windmill / gfx_check_cog.)

---

- Modern controls, part three: gamepads ship with Xbox-style default bindings.
  A fresh install works out of the box, pad entries saved as unbound by older
  versions migrate to the new defaults on load, and the dpad moves clonks too.
  On macOS the event layer now speaks SDL_GameController, so more pads are
  recognized without setup; Windows keeps its DirectInput backend. Linux pad
  support is planned for a future release.
- Two new mission packs ship. The Salt Road is a three-act desert story about a
  caravan, a storm and the scorpion queen; the Escape Rooms are three physics
  escape puzzles with headless-proven solutions.
- Art debt paid down: scorpions animate a real six-phase gait; windmills and
  trade cogs render with real sprites instead of flat placeholders.

---

**A question for players** — the old CCAN archive holds thousands of community
packs, and we are importing the best next. Tell us which you want played again,
in the replies or on the forum: *Ruf der Wipfe* (settlement), *Metall und
Magie*, *Erweiterung Wirtschaft* (economy), *First Aid Pack*, or any other
favourite — the top-rated entries lead our shortlist.

---

*LegacyClonk and this fork:* LegacyClonk is a fan-made continuation of the
Clonk Rage engine originally developed by RedWolf Design. This project is a
friendly fork of the LegacyClonk project; both continue the same engine
independently. Our release numbers count this fork's releases and are not
synchronized with any other project's numbering (fork v371 ≠ upstream vNNN).
Compatibility with existing Clonk Rage content remains a core commitment.
