- Round setup, rebuilt: a reworked two-stage round-setup flow puts the world
  generator first — seed, material amounts, and a live landscape preview —
  then the round's objectives, rules, and winning conditions, each with its
  own slider where the scenario defines a number.
- Winning conditions are adjustable: mode, elimination, and
  cooperative-goal combos plus a settlement-target slider; every
  count-valued rule gets a slider bound to the scenario's Parameters block.
- Network pre-round settings sync: the host's pre-round settings and
  landscape overrides reach every client before the countdown — and leaving
  a network lobby no longer freezes (abort teardown 4.9× faster).
- Real pixel art for nine Agriculture definitions replaces placeholder
  graphics (the placeholder-art debt keeps shrinking).
- An X11 viewport crash class is fixed and a compile-only GUI lane now
  guards it — no more invisible breakage in configurations CI never built.
- Over 100 automated checks (battery, smokes, gfx byte-gates, vision QA)
  run per cycle; this release ships the full settings-rework arc.
