# Fork ↔ upstream statement (DRAFT)

> Status: DRAFT — the full upstream-sync policy lands in cycle 128
> (`fork-upstream-sync-policy`). This statement is deliberately
> factual-neutral: lineage, numbering independence, compatibility. Zero
> policy claims, zero merge-cadence promises.

LegacyClonk is a fan-made continuation of the Clonk Rage engine originally
developed by RedWolf Design. This project is a friendly fork of the
LegacyClonk project; both continue the same engine independently. Our
release numbers count this fork's releases and are not synchronized with
any other project's numbering (fork v369 ≠ upstream vNNN). Compatibility
with existing Clonk Rage content remains a core commitment. — A full
upstream-sync policy is being drafted.
