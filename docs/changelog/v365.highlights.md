- Networking: the engine now binds IPv6 listen sockets correctly on dual-stack hosts.
- Save games: rollback round-trips no longer duplicate landscape objects on reload.
- Replay: scrubbing past the final recorded frame no longer crashes the replay browser.
- Build: reproducible-build pinning landed, so byte-identical binaries can be verified.
